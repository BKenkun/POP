import { Product } from "@/entities";
// This file is now a fallback and can be deprecated,
// but we'll keep it to prevent breaking imports immediately.
// The app now fetches product data from Firestore.

export const cbdProducts: Product[] = [];